# Build / runtime
__pycache__/
*.pyc
*.pyo
.venv/
.python-cache/

# Local SQLite
*.db
master.db

# Local config / runtime state
/config.json
agents/
logs/
.claude/
.claude/settings.local.json
.pre/

# pre runtime per-project state — init/hook 在 pre 仓库自身跑时生成, 不入 git
# (agent_config.json + rules.md 是历史 tracked, 不在此排除)
/pre/next.md
/pre/.done
/pre/.next_action
/pre/transcript_path.txt
/pre/findings/
/pre/reports/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
*.egg-info/
uv.lock
.gemini/
